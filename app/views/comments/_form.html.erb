<%= simple_form_for([event, event.comments.build]) do |f| %>
    <%= f.error_notification %>
    <div class="form-inputs">
      <%= f.text_area :body %>
    </div>
    <%= f.submit "Post",  class: "new" %>
<% end %>
